var Bee = function() {
  Grub.call(this);
  this.age = 5;
  this.color = 'yellow';
  this.job = 'keep on growing';
};

Bee.prototype = Object.create(Grub.prototype); // inherit from Grub
Bee.prototype.constructor = Bee; //point constructor back to itself